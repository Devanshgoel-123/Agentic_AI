import { CustomNotFound } from "@/components/common/CustomNotFoundWidget";

export default function NotFound() {
  return (
    <div style={{ width: "100%", height: "100%" }}>
      <CustomNotFound />
    </div>
  );
}
